# API base URL - points directly to the backend server with /api prefix
# In development: http://localhost:3001/api
# In production: your production API URL (e.g., https://api.example.com/api)
VITE_API_URL=http://localhost:3001/api

# ==============================================
# WordPress Manager - Environment Configuration
# ==============================================
# Copy this file to .env and update the values

# ==============================================
# Database Configuration (PostgreSQL)
# ==============================================
POSTGRES_USER=wordpress_manager
POSTGRES_PASSWORD=your_secure_database_password_here
POSTGRES_DB=wordpress_manager
POSTGRES_PORT=5432

# Database URL for Prisma
# For Docker: This is auto-configured in docker-compose.yml
# For local development without Docker:
DATABASE_URL="postgresql://wordpress_manager:your_password@localhost:5432/wordpress_manager?schema=public"

# For SQLite (development only):
# DATABASE_URL="file:./data/database.db"

# ==============================================
# Application Configuration
# ==============================================
NODE_ENV=production
PORT=3001
APP_PORT=3001

# JWT Secret - MUST be changed in production!
# Generate with: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Encryption Key - MUST be changed in production!
# Generate with: openssl rand -base64 32
ENCRYPTION_KEY=your-super-secret-encryption-key-change-this-in-production

# ==============================================
# File Storage Configuration
# ==============================================
UPLOAD_DIR=./uploads
MAX_FILE_SIZE=10485760

# ==============================================
# WordPress API Configuration
# ==============================================
WP_API_TIMEOUT=30000

# ==============================================
# Optional: pgAdmin Configuration
# ==============================================
# Uncomment to enable pgAdmin (database management UI)
PGADMIN_EMAIL=admin@example.com
PGADMIN_PASSWORD=admin
PGADMIN_PORT=5050

# ==============================================
# Production Deployment Notes
# ==============================================
# 1. Generate secure random values for JWT_SECRET and ENCRYPTION_KEY
# 2. Use strong passwords for POSTGRES_PASSWORD
# 3. Set NODE_ENV=production
# 4. Configure proper backup strategy for postgres_data volume
# 5. Consider using Docker secrets for sensitive values
# 6. Set up SSL/TLS termination (nginx/traefik) in front of the app
# 7. Configure proper logging and monitoring